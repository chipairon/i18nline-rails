<%= ti("thanks", name: "Jeremy") %>

<%= ti("interpolate.from.yml", fruit: "Orange") %>

<%= ti("this.yaml.tell_me") %>

<h1> <%= ti("hello") %> </h1>

<p>This should be <%= ti("translated") %></p>

<p>This is not translated <%= ti ("not.translated.key.with.dots") %></p>

<p>Time now is <%= l(Time.now, format: :long) %></p>

<h1> Problematic elements and alternatives </h1>
<div class="issue-item">
  <div class="issue">
    Translating text on a button (input type=button):
    <br>
    <input type="button" value="<%= ti("button.text") %>">
  </div>
  <div class="alternative">
    <span>Alternative: </span>
    Translating text on a button (button element) should work:
    <br>
    <button><%= ti("button.text") %></button>
  </div>
</div>

